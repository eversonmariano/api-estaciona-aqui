
#LOCALE
spring.web.locale-resolver=fixed
spring.web.locale=pt_BR

# MySQL Database Connection Properties
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/estaciona_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Sao_Paulo
spring.datasource.username=root
spring.datasource.password=1010

# PostgreSQL Database Connection Properties
# O PostgreSQL não fornce parametro de configuração via URL. Por isso, a configuração é realizada pelo
# Hibernate. Outra forma é definir na criação do banco de dados no PostgreSQL o Timezone desejado.
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.hibernate.ddl-auto=update

#Spring doc OpenAPI 3.1 & Swagger 3
springdoc.swagger-ui.path=/docs-estaciona.html
springdoc.api-docs.path=/docs-estaciona
springdoc.packages-to-scan=com.everson.estacionaaqui.web.controller